---
title: 'Tokenization is a Compression Codec Nobody Uses That Way'
date: '2026-05-12'
lastmod: '2026-06-11'
tags: ['compression', 'vector-databases', 'internals', 'nlp']
draft: false
summary: 'Vector databases compress text payloads with LZ4 or gzip at best. Tokenize the text first and entropy-code the token IDs, and you get ~3x lossless compression, twice what gzip manages, using pieces every ML stack already ships.'
images: ['/static/images/blogs/tokenization-compression-og.jpg']
authors: ['default']
---

![Tokenization as Compression Cover](/static/images/blogs/tokenization-compression-og.jpg)

> Looking for TL;DR? Check [key takeaways](#key-takeaways)

Every vector database stores two things for each point: the vector and the payload. Vectors get all the attention: quantization, compression, careful index design. The text in the payload gets LZ4 at best.

This post makes one claim: **tokenize that text before compressing it, and you roughly double what generic byte codecs achieve**. I'll show the benchmark, the latency cost (small), and what it would take to add this to a real database.

## The Forgotten Layer

When you insert a document into Qdrant, Weaviate, or Elasticsearch, you typically store something like:

```json
{
  "id": "doc_123",
  "vector": [0.12, -0.34, ...],
  "payload": {
    "text": "Retrieval-Augmented Generation is a technique..."
  }
}
```

Qdrant stores on-disk payloads in [Gridstore](https://qdrant.tech/articles/gridstore-key-value-storage/) (its custom KV engine, replacing RocksDB since v1.13) with LZ4 compression on by default. Elasticsearch applies LZ4 to stored fields too. Generic byte compression, which on text means roughly 1.3-2x. Neither knows anything about the structure of natural language.

But there is a component that knows a lot about the structure of natural language, and it's already installed in every ML stack: the tokenizer.

## How It Works

<TokenCompressionPipeline />

The top path is what databases do today: store UTF-8 bytes, compress with a generic codec. The bottom path is the proposal: tokenize first, then entropy-code the token IDs.

**Step 1: BPE tokenization.** BPE merges frequent byte sequences into single tokens. " language" is one token. "Retrieval" is three (`Ret + ri + eval`). On average one BPE token covers [3/4 of a word](https://help.openai.com/en/articles/4936856-what-are-tokens-and-how-to-count-them). An average English word is ~5 characters plus a space, so:

```
6 bytes/word × 3/4 words/token = 4.5 bytes/token (raw text)
2 bytes/token (uint16 IDs)
ratio: ~2.25x
```

Just packing token IDs into uint16 already compresses, before any actual compression algorithm runs.

**Step 2: ANS entropy coding.** gzip and LZ4 see opaque bytes; they can't know that `[0x01, 0x7F]` is token 383. An entropy coder working directly on token IDs can give frequent tokens short codes: `" the"` is 40x more common than `"embeddings"`, so it gets a much shorter code. I used ANS (Asymmetric Numeral Systems, the entropy coder inside zstd) via the [constriction](https://github.com/bamler-lab/constriction) library.

One design choice matters here: the frequency table is **trained once on a large corpus and shared**, not built per document. A per-document table compresses better on paper, but the decoder needs the table to reconstruct the document, so you'd have to store it alongside each payload. That's ~900 bytes for a typical 400-token chunk, which erases the entire gain. A shared static table costs nothing per document.

The whole thing is ~30 lines:

```python
import constriction, numpy as np, tiktoken

enc   = tiktoken.get_encoding("r50k_base")
VOCAB = 50_257

# ── one-time setup: train on corpus ──────────────────────────────────────────
corpus_ids = enc.encode(corpus_text)
counts = np.bincount(corpus_ids, minlength=VOCAB).astype(np.float64) + 1  # Laplace
probs  = counts / counts.sum()
model  = constriction.stream.model.Categorical(probs, perfect=False)

# ── per-document encode (store this) ─────────────────────────────────────────
def compress(text):
    ids   = enc.encode(text)
    coder = constriction.stream.stack.AnsCoder()
    coder.encode_reverse(np.array(ids, dtype=np.int32), model)
    return len(ids).to_bytes(4, 'big') + coder.get_compressed().tobytes()

# ── per-document decode ───────────────────────────────────────────────────────
def decompress(data):
    n   = int.from_bytes(data[:4], 'big')
    buf = np.frombuffer(data[4:], dtype=np.uint32).copy()
    ids = constriction.stream.stack.AnsCoder(buf).decode(model, n).tolist()
    return enc.decode(ids)
```

## The Benchmark

A ~270-word paragraph of technical RAG content (1,853 bytes), compressed every way I could think of:

| Method                       |        Size |      Ratio |     CER |
| ---------------------------- | ----------: | ---------: | ------: |
| Raw UTF-8                    | 1,853 bytes |       1.0x |      0% |
| LZ4 (default in Qdrant/ES)   | 1,468 bytes |       1.3x |      0% |
| gzip -9                      |   984 bytes |       1.9x |      0% |
| zstd -22                     |   981 bytes |       1.9x |      0% |
| brotli q=11                  |   711 bytes |       2.6x |      0% |
| r50k BPE uint16 (raw)        |   784 bytes |       2.4x |      0% |
| r50k BPE + zstd              |   709 bytes |       2.6x |      0% |
| zstd dict 112KB (WikiText)   |   747 bytes |       2.5x |      0% |
| **r50k BPE + static ANS**    |   648 bytes | **2.9x** ✓ |      0% |
| mxbai WordPiece + static ANS |  ~630 bytes |        ~3x | 20.1%\* |

\*mxbai's CER drops to 0% after simple post-processing, see the collapsed section below.

CER is character error rate after a full round trip. 0% means lossless: r50k tokenization reconstructs the input byte-for-byte.

Two things stand out. Raw uint16 token IDs, with no compression algorithm at all, already beat every byte codec except brotli. And tokenization + ANS beats everything, on a single small document, where byte codecs are weakest.

The single-document numbers hold up across a corpus. Averaged over 1,773 WikiText-103 test articles:

| Method                     | Avg ratio |
| -------------------------- | --------: |
| gzip -9                    |     1.66x |
| zstd -22                   |     1.68x |
| brotli q=11                |     2.24x |
| zstd dict 112KB (WikiText) |     2.58x |
| **r50k + static ANS**      | **3.35x** |

The fairest competitor is zstd with a corpus-trained dictionary, since it also gets to learn from the corpus. It reaches 2.58x, still 30% behind. The difference is that the token model operates on units of language: `" embeddings"` is one symbol with a known global frequency, where a byte-level model has to rediscover that pattern from scratch.

<PlotlyChart
  data={[
    {
      type: 'bar',
      orientation: 'h',
      x: [1.0, 1.3, 1.9, 1.9, 2.4, 2.5, 2.6, 2.6, 2.9],
      y: [
        'Raw UTF-8 (1,853 B)',
        'LZ4 — Qdrant/ES default (1,468 B)',
        'gzip -9 (984 B)',
        'zstd -22 (981 B)',
        'r50k uint16 raw (784 B)',
        'zstd dict 112KB (747 B)',
        'r50k + zstd (709 B)',
        'brotli q=11 (711 B)',
        'r50k + static ANS (648 B)',
      ],
      marker: {
        color: [
          '#94a3b8',
          '#ef4444',
          '#94a3b8',
          '#94a3b8',
          '#60a5fa',
          '#f59e0b',
          '#60a5fa',
          '#f59e0b',
          '#10b981',
        ],
      },
      text: ['1.0×', '1.3×', '1.9×', '1.9×', '2.4×', '2.5×', '2.6×', '2.6×', '2.9×'],
      textposition: 'outside',
    },
  ]}
  layout={{
    title: { text: 'Compression ratio by method (1,853-byte doc)', font: { size: 14 } },
    xaxis: { title: 'compression ratio (higher is better)', range: [0, 4.0] },
    yaxis: { autorange: 'reversed' },
    height: 380,
    margin: { t: 48, r: 80, b: 48, l: 240 },
    showlegend: false,
  }}
/>

### What does it cost in latency?

Full pipeline, 2,000 runs on the same document:

| Pipeline                       |   Median |   p99 |
| ------------------------------ | -------: | ----: |
| r50k encode (tokenize + ANS)   |    156µs | 287µs |
| r50k decode (ANS + detokenize) | **41µs** |  53µs |

The tokenizer dominates the encode side: 132µs of the 156µs is tiktoken. ANS itself adds ~30µs. If your ingest pipeline already tokenizes (for the embedding model, for token counting), compression is nearly free. Decoding takes 41µs total, which is noise next to a disk read or a network hop.

### Does document length change the picture?

No. Longer documents compress a bit better under every method, and the gap stays put: token + ANS is about 2x better than gzip at every length I tested.

<PlotlyChart
  data={[
    {
      name: 'gzip -9',
      x: [37, 100, 150, 212, 300, 475],
      y: [1.39, 1.62, 1.75, 1.85, 1.93, 2.01],
      mode: 'lines+markers',
      line: { color: '#94a3b8', dash: 'dash' },
      marker: { size: 6, color: '#94a3b8' },
    },
    {
      name: 'zstd -22',
      x: [37, 100, 150, 212, 300, 475],
      y: [1.46, 1.63, 1.76, 1.86, 1.93, 2.03],
      mode: 'lines+markers',
      line: { color: '#64748b', dash: 'dash' },
      marker: { size: 6, color: '#64748b' },
    },
    {
      name: 'r50k + static ANS',
      x: [37, 100, 150, 212, 300, 475],
      y: [3.22, 3.36, 3.39, 3.41, 3.4, 3.51],
      mode: 'lines+markers',
      line: { color: '#10b981' },
      marker: { size: 7, color: '#10b981' },
    },
    {
      name: 'cl100k + static ANS',
      x: [37, 100, 150, 212, 300, 475],
      y: [3.17, 3.3, 3.33, 3.35, 3.34, 3.44],
      mode: 'lines+markers',
      line: { color: '#6366f1' },
      marker: { size: 7, color: '#6366f1' },
    },
  ]}
  layout={{
    xaxis: { title: 'document length (words)', tickvals: [37, 100, 150, 212, 300, 475] },
    yaxis: { title: 'compression ratio', range: [1.0, 4.5] },
    height: 360,
    margin: { t: 16, r: 32, b: 64, l: 64 },
    legend: { orientation: 'h', y: -0.22 },
  }}
/>

<details>
<summary>Side quest: mxbai's WordPiece tokenizer compresses slightly better but lowercases your text. Expand if curious.</summary>

The mixedbread embedding model (`mxbai-embed-large-v1`) uses BERT's WordPiece tokenizer with a 30,522-token vocabulary. Smaller vocabulary means more common tokens, lower entropy, slightly better compression: ~630 bytes vs r50k's 648 on the reference document.

The catch is the 20.1% character error rate. All of it comes from two deterministic quirks of BERT tokenization: lowercasing (`"Qdrant"` → `"qdrant"`) and punctuation spacing (`"(content)"` → `"( content )"`). No information is destroyed; it's just re-spaced. Three regex rules recover the punctuation exactly:

```python
def fix_punct(s):
    s = re.sub(r'\( ', '(', s)
    s = re.sub(r' \)', ')', s)
    s = re.sub(r'(\w) - (\w)', r'\1-\2', s)
    return s
```

Case is genuinely gone, though, and mxbai is also 5-10x slower to tokenize than tiktoken. For a storage codec, r50k is the clean choice. The one real risk with WordPiece is its `[UNK]` token, which destroys information, but on standard English text including code, URLs, and numbers, it only fires on emoji and rare Unicode math symbols.

</details>

## What It Saves at Scale

English Wikipedia is ~7M articles averaging 708 words. Applying the corpus-average ratios:

| Method                |         Per doc |    7M docs |    vs raw |
| --------------------- | --------------: | ---------: | --------: |
| Raw UTF-8             |     4,497 bytes |    31.5 GB |        1x |
| zstd -22              |     2,382 bytes |    16.7 GB |      1.9x |
| **r50k + static ANS** | **1,342 bytes** | **9.4 GB** | **3.35x** |

At a billion documents that's 1.3 TB instead of 4.5 TB: roughly $260/month less on SSD, and every snapshot, replica, and bulk transfer shrinks by the same factor. The I/O win is often worth more than the storage bill, since 3.35x fewer bytes means 3.35x faster bulk reads, indexing, and replication.

One scoping note so the claim doesn't overreach: in collections with large embeddings, the vectors dominate total bytes (a 1536-dim float32 vector is ~6 KB, often bigger than the text next to it). Payload compression matters most when payloads are large, vectors are quantized, or text is the point of the collection.

## How Far Can This Go?

For context, the broader compression landscape (enwik9, 1GB of Wikipedia):

| Method                                   | bits/byte | Ratio vs raw | Practical?     |
| ---------------------------------------- | --------: | -----------: | -------------- |
| gzip -9                                  |      ~2.6 |         3.1x | Yes            |
| zstd -22                                 |      ~2.0 |           4x | Yes            |
| **BPE + static ANS (this post)**         |  **~2.4** |    **~3.3x** | **Yes**        |
| zpaq                                     |      ~1.4 |         5.7x | Slow           |
| fx2-cmix (Hutter Prize record, Oct 2024) |     0.887 |           9x | Extremely slow |
| Chinchilla 70B (DeepMind, 2024)          |     0.664 |          12x | Not practical  |

Everything between 3x and 9x is conditional probability: the static table only knows how often each token appears globally, while the top compressors model `P(token | everything before it)`, which is what language models do.

You don't need a language model for the first step up that ladder, though. A bigram table, `P(token | previous token)`, trained on the same corpus:

| Bigram table size       | Mean doc ratio |
| ----------------------- | -------------: |
| none (unigram baseline) |          3.43x |
| 1.4 MB                  |          4.35x |
| 69 MB (every pair seen) |          4.86x |

These are analytic cross-entropy numbers rather than a running coder, but the method is calibrated: computed the same way, the unigram baseline gives 3.43x where the real ANS coder measured 3.35x, so expect to lose ~2% to coder overhead. Around 4.7x in practice. Tokenizer choice barely matters here; rerunning with cl100k and o200k landed within 1% of r50k. Beyond roughly 5x is genuinely LM territory.

And because the stored format is just token IDs, swapping the unigram table for a bigram table later is a codec upgrade, not a data migration.

## What Would a Database Need to Do?

Nothing exotic. For Qdrant specifically:

1. Accept `encoding: "r50k"` as a payload storage option
2. Tokenize on insert, pack IDs as uint16, run ANS with the shared static table
3. Decompress transparently on read

The index, vectors, and query pipeline don't change at all.

```
Current                          Proposed
───────────────────────────────────────────────────────
INSERT                           INSERT
  payload: {"text": "..."}         payload: {"text": "..."}
       │                                  │
       ▼                                  ▼
  JSON → Gridstore (LZ4)         tokenize → uint16 IDs → ANS
                                        │
                                        ▼
                                  Gridstore (ANS)

READ                             READ
  fetch LZ4 JSON bytes             fetch ANS-compressed bytes
       │                                  │
       ▼                                  ▼
  return as-is                   ANS decode → token IDs → decode
                                        │
                                        ▼
                                  return original text
```

There is one real cost: the vocabulary and frequency table become part of the on-disk format forever, so encodings have to be versioned per collection. A byte codec doesn't carry that obligation.

There's also a bigger-picture reason to like token-native storage as [search and generation converge](https://www.kshivendu.dev/blog/why-search#the-duality-of-search:~:text=Unified%20search%20and%20generation): keeping UTF-8 blobs in the index while the model reasons in tokens means maintaining two representations of the same text. I cover the second-order effects (replication, data-quality filtering, storing LLM output) in [a follow-up post](/blog/token-native-storage).

## Key Takeaways

- Vector databases apply LZ4 to text payloads (1.3x). Tokenizing first and entropy-coding the token IDs gives 3.35x on average. Lossless, about 2x better than gzip.
- Raw uint16 token IDs alone reach 2.4x, beating every byte codec except brotli, with no compression algorithm at all.
- The frequency table must be static and shared. Per-document tables cost ~900 bytes each and erase the gain.
- Latency is a non-issue: ANS adds ~30µs to encoding (the tokenizer you may already run dominates), and decode is ~41µs.
- The same idea stretches to ~4.7x with a bigram table (1.4 MB of table already gets 4.35x). Past ~5x you need an actual language model.
- The trade-off to respect: the vocabulary becomes part of your on-disk format forever, so version your encodings.

## Acknowledgements

The benchmarks use [tiktoken](https://github.com/openai/tiktoken), [zstandard](https://github.com/indygreg/python-zstandard), [lz4](https://github.com/python-lz4/python-lz4), and [constriction](https://github.com/bamler-lab/constriction). Hutter Prize numbers are from [Matt Mahoney's Large Text Compression Benchmark](https://www.mattmahoney.net/dc/text.html).

- https://www.mattmahoney.net/dc/text.html
- https://arxiv.org/abs/2309.10668 (Language Modeling is Compression, DeepMind)
- https://bellard.org/nncp/
- https://github.com/bamler-lab/constriction
- https://github.com/openai/tiktoken
