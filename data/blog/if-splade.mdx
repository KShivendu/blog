---
title: 'Inference-Free SPLADE: Full Quality, 13× Faster Queries'
date: '2026-06-09'
lastmod: '2026-06-09'
tags: ['search', 'sparse-retrieval', 'nlp', 'information-retrieval']
draft: false
summary: 'Inference-free SPLADE nearly matches full SPLADE quality at 13× lower query latency, no GPU needed at query time. Benchmarked against full SPLADE and BM25 on Qdrant and pyserini.'
images: ['/static/images/blogs/if-splade.jpg']
authors: ['default']
---

![Inference Free SPLADE Blog Cover Image](/static/images/blogs/if-splade.jpg)

> Looking for TL;DR? Skip to [key takeaways](#key-takeaways)

BM25 is the workhorse of lexical search. It's fast, requires no GPU, and is surprisingly competitive on most retrieval benchmarks. Its weakness is well-known: it can only match exact terms. A query for "cardiac arrest" won't retrieve a document that says "heart attack", even if that document is the best answer.

SPLADE fixes this by using a neural network to expand documents into a richer sparse representation. Here's what [`naver/splade-v3-doc`](https://huggingface.co/naver/splade-v3-doc) actually activates for "heart attack":

<PlotlyChart
  data={[
    {
      type: 'bar',
      orientation: 'h',
      x: [1.599, 1.133, 0.773, 0.767, 0.764, 0.669, 0.589, 0.498, 0.361, 0.273],
      y: [
        'heart',
        'attack',
        'die',
        'cardiac',
        'attacks',
        'disease',
        'stroke',
        'chest',
        'death',
        'illness',
      ],
      marker: { color: '#10b981' },
      text: [1.599, 1.133, 0.773, 0.767, 0.764, 0.669, 0.589, 0.498, 0.361, 0.273].map((v) =>
        v.toFixed(2)
      ),
      textposition: 'outside',
    },
  ]}
  layout={{
    title: { text: 'SPLADE token activations for "heart attack"', font: { size: 14 } },
    xaxis: { title: 'activation weight', range: [0, 2.1] },
    yaxis: { autorange: 'reversed' },
    height: 320,
    margin: { t: 48, r: 64, b: 48, l: 80 },
    showlegend: false,
  }}
/>

The original tokens score highest, but semantically related terms like `cardiac`, `stroke`, `disease`, `chest`, `death` are all activated with meaningful weights. A query for "cardiac arrest" hits `cardiac` (weight 0.77) even though the document only mentions "heart attack." BM25 would miss it entirely.

The catch is query latency: running a transformer at search time adds 50–100ms per query.

**Inference-free SPLADE** removes that catch entirely. The model runs only at index time on documents. Queries use raw BERT token IDs: no model, no GPU, no neural inference. You pay once upfront. Queries stay fast.

Here's how it compares across six retrieval setups, with one result that surprised me.

## How It Works

<SpladeVsIF />

The document vectors still carry neural expansions: "heart" activates "cardiac", "coronary", "myocardial". The query side is just tokenization: "cardiac arrest" becomes token IDs `[3684, 6295]` each with weight 1.0. No model call, no GPU.

```python
def encode_query_inference_free(tokenizer, query: str):
    enc = tokenizer(query, add_special_tokens=False, truncation=True, max_length=512)
    unique_ids = list(set(enc["input_ids"]))
    return SparseVector(indices=unique_ids, values=[1.0] * len(unique_ids))
```

At query time: one tokenizer call + one sparse dot product. That's it.

## Results

I benchmarked a full matrix of configurations on [BEIR scifact](https://github.com/beir-cellar/beir) (5,183 docs, 300 queries, scientific claim verification): three SPLADE models × two backends × full and inference-free mode, plus BM25 on both backends. SPLADE doc encoding ran on a Modal A10G GPU. [`naver/splade-v3`](https://huggingface.co/naver/splade-v3) and [`naver/splade-v3-doc`](https://huggingface.co/naver/splade-v3-doc) are the same model family (symmetric vs asymmetric training), giving the cleanest apples-to-apples measure of dropping query-side inference. The third model, [`opensearch-neural-sparse-encoding-doc-v3-gte`](https://huggingface.co/opensearch-project/opensearch-neural-sparse-encoding-doc-v3-gte), is an asymmetric doc-only encoder from OpenSearch.

**Qdrant** (native float sparse scoring)

<div style={{overflowX: 'auto'}}>

| Method                 | NDCG@10    | Recall@10  | MRR@10     | Total (median/p90) | Query embed | Search    |
| ---------------------- | ---------- | ---------- | ---------- | ------------------ | ----------- | --------- |
| SPLADE-Full (naver)    | **0.7161** | 0.8289     | **0.6886** | 57.2ms / 79.0ms    | 50.0ms      | 6.6ms     |
| SPLADE-Full (PP)       | 0.7093     | 0.8240     | 0.6811     | 60.4ms / 88.8ms    | 53.2ms      | 7.1ms     |
| SPLADE-IF (naver)      | 0.7068     | **0.8254** | 0.6748     | **4.3ms / 5.7ms**  | **0.3ms**   | **4.0ms** |
| SPLADE-IF (OpenSearch) | 0.7021     | 0.8220     | 0.6717     | 4.3ms / 5.7ms      | 0.3ms       | 4.0ms     |
| SPLADE-IF (PP, sym)    | 0.6859     | 0.8121     | 0.6507     | 3.9ms / 5.7ms      | 0.3ms       | 3.6ms     |
| BM25                   | 0.6830     | 0.8088     | 0.6470     | 4.0ms / 5.0ms      | 0.1ms       | 3.8ms     |

</div>

<PlotlyChart
  data={[
    {
      name: 'SPLADE-Full',
      x: [57.2, 60.4],
      y: [0.7161, 0.7093],
      mode: 'markers+text',
      type: 'scatter',
      text: ['naver', 'PP'],
      textposition: ['top left', 'bottom left'],
      marker: { size: 13, color: '#6366f1', line: { width: 2, color: 'white' } },
      textfont: { size: 11 },
    },
    {
      name: 'SPLADE-IF',
      x: [4.3, 4.29, 3.9],
      y: [0.7068, 0.7021, 0.6859],
      mode: 'markers+text',
      type: 'scatter',
      text: ['naver', 'GTE', 'PP-sym'],
      textposition: ['top right', 'bottom right', 'top left'],
      marker: { size: 13, color: '#10b981', symbol: 'star', line: { width: 1, color: 'white' } },
      textfont: { size: 11 },
    },
    {
      name: 'BM25',
      x: [4.0],
      y: [0.683],
      mode: 'markers+text',
      type: 'scatter',
      text: ['BM25'],
      textposition: ['top right'],
      marker: { size: 11, color: '#f59e0b', symbol: 'square', line: { width: 2, color: 'white' } },
      textfont: { size: 11 },
    },
  ]}
  layout={{
    title: { text: 'Quality vs Query Latency (BEIR scifact)', font: { size: 15 } },
    xaxis: {
      title: { text: 'Median query latency (ms), lower is better →', standoff: 12 },
      type: 'log',
      tickmode: 'array',
      tickvals: [2, 3, 4, 5, 10, 20, 60, 80],
      ticktext: ['2', '3', '4', '5', '10', '20', '60', '80'],
      range: [0.3, 2.0],
    },
    yaxis: { title: 'NDCG@10 ↑ higher is better', range: [0.62, 0.74] },
    height: 460,
    showlegend: true,
    legend: { orientation: 'h', y: -0.18 },
    margin: { t: 56, r: 48, b: 80, l: 64 },
  }}
/>

The 13× latency gap comes entirely from eliminating the query encoder call. Search time is unchanged.

<PlotlyChart
  data={[
    {
      name: 'Query embed',
      x: ['BM25', 'IF (naver)', 'IF (GTE)', 'IF (PP-sym)', 'Full (naver)', 'Full (PP)'],
      y: [0.1, 0.3, 0.3, 0.3, 50.0, 53.2],
      type: 'bar',
      marker: { color: '#6366f1' },
    },
    {
      name: 'Search',
      x: ['BM25', 'IF (naver)', 'IF (GTE)', 'IF (PP-sym)', 'Full (naver)', 'Full (PP)'],
      y: [3.8, 4.0, 4.0, 3.6, 6.6, 7.1],
      type: 'bar',
      marker: { color: '#10b981' },
    },
  ]}
  layout={{
    title: {
      text: 'Total Query Latency = Embed + Search (median ms)',
      font: { size: 15 },
    },
    barmode: 'stack',
    xaxis: { title: '' },
    yaxis: { title: 'ms', tickformat: '.0f' },
    height: 380,
    margin: { t: 56, r: 24, b: 64, l: 64 },
    legend: { orientation: 'h', y: -0.22 },
  }}
/>

I also ran everything on Lucene/pyserini. Numbers come out 1–2% lower. Lucene stores doc weights as integers, so you lose some float precision. OpenSearch and Elasticsearch have the same constraint. None of them support exact float sparse scoring. The tradeoff is fine: integer arithmetic speeds up search.

<details>
<summary>**Lucene/pyserini results** (integer-quantized impact scoring, `JsonVectorCollection --impact`)</summary>

<div style={{overflowX: 'auto'}}>

| Method                 | NDCG@10    | Recall@10 | MRR@10     | Total (median/p90) | Query embed | Search    |
| ---------------------- | ---------- | --------- | ---------- | ------------------ | ----------- | --------- |
| SPLADE-Full (naver)    | **0.7156** | 0.8272    | **0.6889** | 57.9ms / 82.8ms    | 47.8ms      | 9.8ms     |
| SPLADE-Full (PP)       | 0.6969     | 0.8056    | 0.6704     | 59.3ms / 87.1ms    | 51.9ms      | 7.4ms     |
| SPLADE-IF (naver)      | 0.6953     | 0.8039    | 0.6706     | **3.2ms / 4.7ms**  | **0.2ms**   | **3.0ms** |
| SPLADE-IF (OpenSearch) | 0.6886     | 0.8087    | 0.6593     | 2.5ms / 3.5ms      | 0.2ms       | 2.3ms     |
| SPLADE-IF (PP, sym)    | 0.6704     | 0.7954    | 0.6367     | 2.4ms / 3.0ms      | 0.2ms       | 2.3ms     |
| BM25                   | 0.6789     | 0.8038    | 0.6457     | 5.5ms / 8.9ms      | 0.0ms       | 5.5ms     |

</div>

</details>

## SPLADE-IF Nearly Matches Full SPLADE at 13× Lower Latency

Comparing [`naver/splade-v3`](https://huggingface.co/naver/splade-v3) (full) vs [`naver/splade-v3-doc`](https://huggingface.co/naver/splade-v3-doc) (inference-free) in the same model family: dropping query-side inference costs **−0.0093 NDCG@10** (under 1.3% relative). The latency difference is stark: full SPLADE costs **57ms median** per query vs **4.3ms** for SPLADE-IF, a 13× gap, entirely from the 50ms query encoder call.

Why does inference-free work so well? [`naver/splade-v3-doc`](https://huggingface.co/naver/splade-v3-doc) is an **asymmetric model** trained knowing that queries will be raw tokens with no weighting. So the doc encoder compensates: it expands aggressively at index time, and the raw query terms land on vocabulary that was prepared for them.

You can see this in the sparsity numbers: SPLADE-IF doc vectors average 314 non-zeros vs 288 for the full model. The doc side just carries more. Full SPLADE splits the expansion work between query and doc. Inference-free pushes it all into the doc. Both get to similar quality.

Both methods beat BM25-Qdrant meaningfully: **+3.5% NDCG@10, +2.1% Recall@10, +4.3% MRR@10** for SPLADE-IF, at essentially the same 4ms query latency.

## The Tradeoff: Free Queries, Expensive Index

At query time, SPLADE-IF and BM25 are the same operation: a **sparse dot product** in Qdrant. Both take ~4ms. The "inference-free" name can mislead. There's no neural model removed from the query path relative to BM25, just relative to full SPLADE.

The cost is entirely upfront. SPLADE-IF models encode at 58–83 docs/sec on an A10G GPU vs BM25's 1,439 docs/sec on CPU, about 17–25× slower. At that rate, a million-document corpus takes 3–5 hours. For a static or slowly-updated corpus this is a one-time cost. For high-churn indexes you'd need an async re-encoding pipeline, the same problem dense vector search already solves.

Once indexed: **encode once, query forever.**

<PlotlyChart
  data={[
    {
      name: 'Doc encoding throughput (docs/sec)',
      x: ['BM25', 'SPLADE-Full', 'IF-naver', 'IF-GTE'],
      y: [1439, 98, 83, 58],
      type: 'bar',
      marker: { color: ['#f59e0b', '#6366f1', '#10b981', '#34d399'] },
      text: ['1,439 (CPU)', '98 (GPU)', '83 (GPU)', '58 (GPU)'],
      textposition: 'outside',
    },
  ]}
  layout={{
    title: { text: 'Doc Encoding Throughput at Index Time', font: { size: 15 } },
    xaxis: { title: '' },
    yaxis: {
      title: 'docs / sec',
      type: 'log',
      range: [1.7, 3.7],
      tickmode: 'array',
      tickvals: [100, 1000],
      ticktext: ['100', '1,000'],
    },
    height: 360,
    margin: { t: 56, r: 24, b: 64, l: 72 },
    showlegend: false,
  }}
/>

## Limitations

**Model choice matters.** [`naver/splade-v3-doc`](https://huggingface.co/naver/splade-v3-doc) was trained for raw-token queries. Its doc encoder compensates for the missing query expansion. A symmetric model like [`prithivida/Splade_PP_en_v1`](https://huggingface.co/prithivida/Splade_PP_en_v1) still beats BM25 in IF mode, but costs ~2% NDCG@10, a real quality hit for a free speedup.

**No query-time adaptation.** The doc encoder must anticipate every useful expansion upfront. On truly novel terms (new drug names, recent product launches, breaking-news proper nouns), SPLADE-IF degrades toward BM25 behavior. Full SPLADE handles this better via query-time expansion.

**Domain sensitivity.** IF works best when vocabulary bridging is the core challenge and domain vocabulary is predictable (scientific, medical, legal). On e-commerce, where product titles already closely match query terms, document-side expansion helps less and the quality gap over BM25 narrows significantly.

**Denser index, GPU required at index time.** SPLADE-IF doc vectors are denser than full SPLADE (314 vs 205 avg non-zeros), and encoding requires a GPU. Both are one-time costs for static corpora. See the Tradeoff section above.

## Key Takeaways

- **SPLADE-IF nearly matches full SPLADE quality.** Within the same model family, dropping query-side inference costs only 0.0093 NDCG@10 (under 1.3% relative) while cutting query latency **13×** (57ms → 4.3ms). Both beat BM25 by +3.5% NDCG@10 at essentially the same latency.
- **Use an asymmetric model.** A symmetric model in IF mode still beats BM25, but costs ~2% NDCG@10 vs a model trained for it ([naver/splade-v3-doc](https://huggingface.co/naver/splade-v3-doc)). If you're starting fresh, pick the asymmetric one.
- **The only real tradeoff is index time.** Queries are free once indexed. The GPU cost at index time (83 docs/sec on A10G) is acceptable for static corpora. High-churn indexes need an async re-encoding pipeline.
