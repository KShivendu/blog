---
title: 'Static Embeddings: Inference-Free Dense Retrieval'
date: '2026-06-29'
lastmod: '2026-06-29'
tags: ['search', 'embeddings', 'nlp', 'information-retrieval']
draft: true
summary: 'Static embeddings drop the transformer entirely — a token lookup plus a mean. The retrieval-tuned potion-retrieval-32M hits 98% of all-MiniLM quality on scifact at ~90× lower query latency, all on CPU. Across 13 NanoBEIR datasets it stays roughly BM25-competitive (≈92% of its mean NDCG) with a mild, statistically-weak tendency to win on low-lexical-overlap tasks. One hard catch: they are mathematically blind to word order.'
images: ['/static/images/blogs/if-splade.jpg']
authors: ['default']
---

> Looking for TL;DR? Skip to [key takeaways](#key-takeaways)

In [Inference-Free SPLADE](/blog/if-splade) I removed the transformer from the _query_ side of sparse search: documents still get neural expansion at index time, but queries are just tokenized. The result was full SPLADE quality at 13× lower latency.

Static embeddings push that idea to its limit on the _dense_ side: there is no transformer at query time **or** index time. A document vector is computed by looking up one vector per token from a fixed table and averaging them. No attention, no forward pass — `O(tokens)` table lookups plus a mean.

This is conceptually word2vec/GloVe, but the lookup tables are **distilled or trained from modern sentence transformers**, so they're far better than classic word vectors. Two families dominate today:

- [**Model2Vec**](https://github.com/MinishLab/model2vec) — distills a sentence transformer into a static table via a forward pass over the vocabulary, then PCA and Zipf re-weighting. [`potion-base-32M`](https://huggingface.co/minishlab/potion-base-32M) is distilled from BGE-base on **C4 web text** (general-purpose); [`potion-retrieval-32M`](https://huggingface.co/minishlab/potion-retrieval-32M) then fine-tunes that on a **retrieval mix (MS MARCO, GooAQ, SQuAD, S2ORC, NQ, TriviaQA, PubMedQA…)**.
- [**`sentence-transformers/static-retrieval-mrl-en-v1`**](https://huggingface.co/sentence-transformers/static-retrieval-mrl-en-v1) — a _trained_ `EmbeddingBag` ([Tom Aarsen's recipe](https://huggingface.co/blog/static-embeddings)) with [Matryoshka](https://huggingface.co/blog/matryoshka) dimensions (1024 → 32), trained on **80M retrieval pairs** from the same QA/MS MARCO family.

The training data matters: it tells you where each model should be strong (QA, paraphrase, low lexical overlap) and where it won't be. I benchmarked them against a full transformer dense baseline and BM25 — first in detail on scifact, then across **four hand-picked BEIR datasets**, then rigorously across **all 13 of NanoBEIR** with bootstrap CIs and significance tests. The speedups are enormous; the quality story is more nuanced than the tidy version, and I'll show where it breaks.

## How It Works

<StaticPipeline />

The only difference from a normal sentence transformer is the middle. A transformer runs 6–12 attention layers; a static model replaces that entire stack with a single lookup table. Everything else — tokenize, pool, cosine — is identical.

```python
from model2vec import StaticModel

model = StaticModel.from_pretrained("minishlab/potion-retrieval-32M")
# encode = tokenize → look up one row per token → mean-pool. No transformer.
vecs = model.encode(["heart attack", "cardiac arrest"])
```

Because there's no forward pass, this runs at thousands of documents per second on a CPU. The price you pay is in what a lookup-then-average _can't_ represent — more on that below.

## Results on scifact

I started on [BEIR scifact](https://github.com/beir-cellar/beir) (5,183 docs, 300 test queries, scientific claim verification) — the same dataset as the SPLADE post. Exact brute-force cosine search, everything on CPU. Query latency is per query (batch size 1), the real query path.

<div style={{overflowX: 'auto'}}>

| Method                     | Type        | NDCG@10    | Recall@10  | MRR@10     | Dim  | Query embed | Index throughput |
| -------------------------- | ----------- | ---------- | ---------- | ---------- | ---- | ----------- | ---------------- |
| bge-small-en-v1.5          | transformer | **0.7215** | **0.8396** | **0.6891** | 384  | 21.6ms      | 6.5 docs/s       |
| BM25                       | lexical     | 0.6645     | 0.7849     | 0.6325     | —    | —           | 6,360 docs/s     |
| all-MiniLM-L6-v2           | transformer | 0.6484     | 0.7883     | 0.6068     | 384  | 12.1ms      | 19 docs/s        |
| **potion-retrieval-32M**   | **static**  | 0.6345     | 0.7948     | 0.5912     | 512  | **0.13ms**  | **1,562 docs/s** |
| static-retrieval-mrl-en-v1 | static      | 0.5935     | 0.7295     | 0.5568     | 1024 | 0.39ms      | 1,126 docs/s     |
| potion-base-32M            | static      | 0.5618     | 0.7024     | 0.5246     | 512  | 0.12ms      | 1,571 docs/s     |

</div>

Two things jump out. First, **the retrieval-tuned static model is far better than the general one**: `potion-retrieval-32M` (0.6345) beats its own base `potion-base-32M` (0.5618) by **13%**, just from fine-tuning the same table on retrieval data. Always use the retrieval variant.

Second, the headline tradeoff: `potion-retrieval-32M` reaches **97.9% of all-MiniLM's NDCG@10** (0.6345 vs 0.6484) — and actually **beats it on Recall@10** (0.7948 vs 0.7883) — while embedding queries **~90× faster** (0.13ms vs 12.1ms) and indexing **~80× faster** (1,562 vs 19 docs/sec). It lands within 4.5% of BM25, at a query cost indistinguishable from BM25's.

<PlotlyChart
  data={[
    {
      name: 'Transformer dense',
      x: [21.6, 12.1],
      y: [0.7215, 0.6484],
      mode: 'markers+text',
      type: 'scatter',
      text: ['bge-small', 'all-MiniLM'],
      textposition: ['top left', 'bottom left'],
      marker: { size: 13, color: '#6366f1', line: { width: 2, color: 'white' } },
      textfont: { size: 11 },
    },
    {
      name: 'Static',
      x: [0.13, 0.39, 0.12],
      y: [0.6345, 0.5935, 0.5618],
      mode: 'markers+text',
      type: 'scatter',
      text: ['potion-retrieval', 'static-mrl', 'potion-base'],
      textposition: ['top right', 'bottom right', 'bottom right'],
      marker: { size: 14, color: '#10b981', symbol: 'star', line: { width: 1, color: 'white' } },
      textfont: { size: 11 },
    },
  ]}
  layout={{
    title: { text: 'Quality vs Query Latency (BEIR scifact, CPU)', font: { size: 15 } },
    xaxis: {
      title: { text: 'Median query embed latency (ms), lower is better →', standoff: 12 },
      type: 'log',
      tickmode: 'array',
      tickvals: [0.1, 0.2, 0.4, 1, 2, 5, 12, 22],
      ticktext: ['0.1', '0.2', '0.4', '1', '2', '5', '12', '22'],
      range: [-1.1, 1.5],
    },
    yaxis: { title: 'NDCG@10 ↑ higher is better', range: [0.5, 0.75] },
    shapes: [
      {
        type: 'line',
        x0: -1.1,
        x1: 1.5,
        xref: 'x',
        y0: 0.6644,
        y1: 0.6644,
        yref: 'y',
        line: { color: '#f59e0b', width: 1.5, dash: 'dash' },
      },
    ],
    annotations: [
      {
        x: 1.5,
        y: 0.6644,
        xref: 'x',
        yref: 'y',
        text: 'BM25 = 0.664',
        showarrow: false,
        font: { size: 11, color: '#f59e0b' },
        xanchor: 'right',
        yanchor: 'bottom',
      },
    ],
    height: 440,
    showlegend: true,
    legend: { orientation: 'h', y: -0.2 },
    margin: { t: 56, r: 32, b: 80, l: 64 },
  }}
/>

The latency gap is even more dramatic at index time. Static models encode in the same ballpark as BM25's tokenizer; transformers are two-to-three orders of magnitude slower on CPU:

<PlotlyChart
  data={[
    {
      x: ['potion-retrieval', 'static-mrl', 'all-MiniLM', 'bge-small'],
      y: [1562, 1126, 19, 6.5],
      type: 'bar',
      marker: { color: ['#10b981', '#34d399', '#6366f1', '#818cf8'] },
      text: ['1,562', '1,126', '19', '6.5'],
      textposition: 'outside',
    },
  ]}
  layout={{
    title: { text: 'Document Encoding Throughput (docs/sec, CPU)', font: { size: 15 } },
    xaxis: { title: '' },
    yaxis: {
      title: 'docs / sec',
      type: 'log',
      range: [0.6, 3.4],
      tickmode: 'array',
      tickvals: [10, 100, 1000],
      ticktext: ['10', '100', '1,000'],
    },
    height: 350,
    margin: { t: 56, r: 24, b: 48, l: 64 },
    showlegend: false,
  }}
/>

## Where Static Embeddings Actually Win

scifact is one dataset, and a lexically dense one — queries and relevant documents share rare, specific terms (gene names, conditions), which is exactly BM25's strength. To see where static dense pays off, I ran the same benchmark on three more BEIR datasets spanning the lexical-overlap spectrum: [**arguana**](https://huggingface.co/datasets/BeIR/arguana) (argument → counter-argument, almost no shared vocabulary), [**nfcorpus**](https://huggingface.co/datasets/BeIR/nfcorpus) (medical), and [**fiqa**](https://huggingface.co/datasets/BeIR/fiqa) (financial QA). Transformer corpora were encoded on a Modal A10G; static and BM25 stayed on CPU.

<div style={{overflowX: 'auto'}}>

| Model (NDCG@10)          | scifact    | nfcorpus   | **arguana** | fiqa   |
| ------------------------ | ---------- | ---------- | ----------- | ------ |
| BM25                     | **0.6645** | **0.3105** | 0.3152      | 0.2318 |
| **potion-retrieval-32M** | 0.6345     | 0.3076     | **0.3327**  | 0.1885 |
| static-retrieval-mrl     | 0.5935     | 0.3004     | **0.3314**  | 0.1996 |
| all-MiniLM-L6-v2         | 0.6484     | 0.3179     | 0.3664      | 0.3637 |
| bge-small-en-v1.5        | 0.7215     | 0.3402     | 0.4305      | 0.3884 |

</div>

There's a suggestive pattern here: **a static embedding's win over BM25 seems to track how little lexical overlap the task has** — static wins on low-overlap arguana, ties on nfcorpus, loses on lexically dense scifact/fiqa. It's a tidy story on four hand-picked datasets. The [next section](#does-the-pattern-hold-13-datasets-with-statistics) tests it properly across 13 — and it only half-survives.

<PlotlyChart
  data={[
    {
      name: 'BM25',
      x: ['arguana', 'nfcorpus', 'scifact', 'fiqa'],
      y: [0.3152, 0.3105, 0.6645, 0.2318],
      type: 'bar',
      marker: { color: '#f59e0b' },
    },
    {
      name: 'potion-retrieval-32M (static)',
      x: ['arguana', 'nfcorpus', 'scifact', 'fiqa'],
      y: [0.3327, 0.3076, 0.6345, 0.1885],
      type: 'bar',
      marker: { color: '#10b981' },
    },
    {
      name: 'all-MiniLM-L6-v2 (transformer)',
      x: ['arguana', 'nfcorpus', 'scifact', 'fiqa'],
      y: [0.3664, 0.3179, 0.6484, 0.3637],
      type: 'bar',
      marker: { color: '#6366f1' },
    },
  ]}
  layout={{
    title: { text: 'Static vs BM25 vs transformer, by dataset (NDCG@10)', font: { size: 14 } },
    barmode: 'group',
    xaxis: { title: '← lower lexical overlap · higher lexical overlap →' },
    yaxis: { title: 'NDCG@10' },
    height: 400,
    legend: { orientation: 'h', y: -0.18 },
    margin: { t: 48, r: 24, b: 72, l: 56 },
  }}
/>

- **arguana** — queries are arguments, relevant docs are _counter-arguments_ that rarely reuse the query's words. BM25 has little to grip. Here **both static models beat BM25** (potion-retrieval 0.333 vs 0.315, +5.6%) — the clean win, at ~0.1ms/query.
- **nfcorpus** — static essentially **ties** BM25 (0.308 vs 0.311).
- **scifact / fiqa** — lexically dense; BM25 wins. On fiqa, financial jargon overlaps heavily and a real transformer (MiniLM 0.364) is needed to beat BM25 — static (0.20) can't.

So the first read: a static embedding buys you **~90× lower query latency**, wins on some tasks, loses on others. But four datasets I chose to span the spectrum is exactly the setup that manufactures a clean trend. Does it actually hold?

## Does the Pattern Hold? 13 Datasets With Statistics

I tested the lexical-overlap hypothesis properly on all of [**NanoBEIR**](https://huggingface.co/collections/zeta-alpha-ai/nanobeir-66e1a0af21dfd93e620cd9f6) — 13 datasets, 50 queries each, the benchmark the static-mrl model card itself cites. For each I computed a **lexical-overlap score** (mean fraction of a query's content words that appear in its relevant documents) and the **Δ = NDCG@10(potion-retrieval) − NDCG@10(BM25)**, with bootstrap 95% CIs and a per-query paired Wilcoxon test. The hypothesis predicts Δ falls as overlap rises.

<PlotlyChart
  data={[
    {
      name: 'tied with BM25 (p ≥ 0.05)',
      x: [0.151, 0.482, 0.501, 0.511, 0.579, 0.611, 0.643, 0.673, 0.863],
      y: [0.0437, 0.0073, -0.0264, -0.0335, 0.0145, -0.0332, -0.0798, 0.0068, 0.0038],
      mode: 'markers+text',
      type: 'scatter',
      text: [
        'ArguAna',
        'ClimateFEVER',
        'NQ',
        'SciFact',
        'FiQA',
        'SCIDOCS',
        'MSMARCO',
        'NFCorpus',
        'DBPedia',
      ],
      textposition: 'top center',
      textfont: { size: 9 },
      marker: { size: 10, color: '#94a3b8', symbol: 'circle-open', line: { width: 2 } },
    },
    {
      name: 'significant (p < 0.05)',
      x: [0.704, 0.726, 0.768, 0.989],
      y: [-0.2251, 0.1089, -0.1441, -0.1956],
      mode: 'markers+text',
      type: 'scatter',
      text: ['HotpotQA', 'Quora', 'FEVER', 'Touche2020'],
      textposition: 'top center',
      textfont: { size: 9 },
      marker: { size: 12, color: '#10b981', symbol: 'circle', line: { width: 1, color: 'white' } },
    },
    {
      name: 'trend (r = −0.47)',
      x: [0.1, 1.0],
      y: [0.0739, -0.1235],
      mode: 'lines',
      type: 'scatter',
      line: { color: '#6366f1', width: 2, dash: 'dash' },
    },
  ]}
  layout={{
    title: { text: 'Static-minus-BM25 NDCG@10 vs lexical overlap (NanoBEIR)', font: { size: 14 } },
    xaxis: { title: 'lexical overlap (query terms found in relevant docs) →' },
    yaxis: {
      title: 'Δ NDCG@10  (static − BM25)',
      zeroline: true,
      zerolinecolor: '#f59e0b',
      zerolinewidth: 2,
    },
    height: 440,
    legend: { orientation: 'h', y: -0.2 },
    margin: { t: 48, r: 24, b: 72, l: 64 },
  }}
/>

The trend is real but **weak and not significant**: Spearman ρ = **−0.43** (p = 0.14), Pearson r = −0.47 over 13 points. I'd pre-registered "confirmed" as ρ ≤ −0.5 at p < 0.05 — this misses it. Three honest conclusions:

- **The extremes behave as predicted.** Touche2020 (overlap 0.99) and HotpotQA (0.70, multi-hop — relevant docs are bridge passages, not lexical matches) hand BM25 big, significant wins; arguana (0.15) tilts static. But the middle is noise.
- **Quora breaks the rule outright.** High lexical overlap (0.73), yet static _beats_ BM25 by +0.11 (p < 0.01). Duplicate-question retrieval is semantic even when the words match — so word overlap isn't the mechanism, it's a correlate. Task _type_ matters more than the overlap number.
- **On most single datasets, static and BM25 are statistically tied.** Only 4 of 13 reach p < 0.05 with 50 queries. And averaged across all 13, BM25 (mean NDCG@10 **0.546**) edges potion-retrieval (**0.504**) and all-MiniLM (0.562); bge-small leads at 0.609.

So the stronger, defensible claim is narrower than the four-dataset chart suggested: **static dense is roughly BM25-competitive on average (≈92% of its mean NDCG) at ~90× lower latency, with a mild tendency to win on low-lexical-overlap / semantic tasks** — not a clean law you can read off an overlap score. If relevance is paraphrase- or duplicate-driven (Quora, arguana), static is a strong, cheap default; if it's multi-hop or term-precise (HotpotQA, Touche, scifact), reach for BM25 or a real transformer.

<details>
<summary>**Full NanoBEIR table** — NDCG@10 with bootstrap 95% CIs, all 13 datasets sorted by lexical overlap</summary>

<div style={{overflowX: 'auto'}}>

| Dataset        | overlap | BM25                             | potion-retrieval-32M             | static-mrl                   | all-MiniLM-L6-v2             | bge-small-en-v1.5            |
| -------------- | ------- | -------------------------------- | -------------------------------- | ---------------------------- | ---------------------------- | ---------------------------- |
| ArguAna        | 0.15    | 0.365 <sub>[0.28,0.45]</sub>     | 0.408 <sub>[0.32,0.49]</sub>     | 0.407 <sub>[0.32,0.49]</sub> | 0.549 <sub>[0.47,0.63]</sub> | 0.636 <sub>[0.55,0.72]</sub> |
| ClimateFEVER   | 0.48    | 0.328 <sub>[0.25,0.40]</sub>     | 0.335 <sub>[0.25,0.42]</sub>     | 0.331 <sub>[0.25,0.41]</sub> | 0.296 <sub>[0.22,0.37]</sub> | 0.305 <sub>[0.23,0.39]</sub> |
| NQ             | 0.50    | 0.469 <sub>[0.36,0.58]</sub>     | 0.443 <sub>[0.34,0.54]</sub>     | 0.453 <sub>[0.35,0.56]</sub> | 0.590 <sub>[0.48,0.70]</sub> | 0.532 <sub>[0.42,0.64]</sub> |
| SciFact        | 0.51    | 0.717 <sub>[0.62,0.81]</sub>     | 0.683 <sub>[0.57,0.79]</sub>     | 0.611 <sub>[0.50,0.72]</sub> | 0.727 <sub>[0.63,0.82]</sub> | 0.755 <sub>[0.64,0.85]</sub> |
| FiQA2018       | 0.58    | 0.352 <sub>[0.26,0.44]</sub>     | 0.366 <sub>[0.27,0.47]</sub>     | 0.365 <sub>[0.27,0.47]</sub> | 0.477 <sub>[0.38,0.58]</sub> | 0.471 <sub>[0.38,0.57]</sub> |
| SCIDOCS        | 0.61    | 0.338 <sub>[0.27,0.41]</sub>     | 0.305 <sub>[0.24,0.38]</sub>     | 0.264 <sub>[0.19,0.34]</sub> | 0.433 <sub>[0.36,0.50]</sub> | 0.414 <sub>[0.33,0.49]</sub> |
| MSMARCO        | 0.64    | 0.490 <sub>[0.39,0.60]</sub>     | 0.410 <sub>[0.31,0.51]</sub>     | 0.404 <sub>[0.30,0.50]</sub> | 0.554 <sub>[0.44,0.65]</sub> | 0.619 <sub>[0.50,0.72]</sub> |
| NFCorpus       | 0.67    | 0.328 <sub>[0.24,0.43]</sub>     | 0.335 <sub>[0.24,0.44]</sub>     | 0.324 <sub>[0.23,0.43]</sub> | 0.332 <sub>[0.24,0.43]</sub> | 0.370 <sub>[0.27,0.49]</sub> |
| HotpotQA       | 0.70    | **0.827** <sub>[0.77,0.88]</sub> | 0.602 <sub>[0.52,0.68]</sub>     | 0.655 <sub>[0.57,0.73]</sub> | 0.596 <sub>[0.51,0.68]</sub> | 0.818 <sub>[0.75,0.88]</sub> |
| QuoraRetrieval | 0.73    | 0.788 <sub>[0.70,0.87]</sub>     | **0.897** <sub>[0.84,0.95]</sub> | 0.895 <sub>[0.84,0.94]</sub> | 0.937 <sub>[0.88,0.97]</sub> | 0.953 <sub>[0.91,0.98]</sub> |
| FEVER          | 0.77    | 0.818 <sub>[0.72,0.90]</sub>     | 0.673 <sub>[0.59,0.76]</sub>     | 0.692 <sub>[0.61,0.77]</sub> | 0.793 <sub>[0.72,0.87]</sub> | 0.921 <sub>[0.87,0.97]</sub> |
| DBPedia        | 0.86    | 0.561 <sub>[0.48,0.63]</sub>     | 0.565 <sub>[0.50,0.63]</sub>     | 0.568 <sub>[0.49,0.64]</sub> | 0.550 <sub>[0.48,0.63]</sub> | 0.580 <sub>[0.50,0.66]</sub> |
| Touche2020     | 0.99    | **0.723** <sub>[0.68,0.76]</sub> | 0.527 <sub>[0.47,0.58]</sub>     | 0.570 <sub>[0.51,0.63]</sub> | 0.475 <sub>[0.41,0.54]</sub> | 0.540 <sub>[0.47,0.61]</sub> |
| **mean**       | —       | **0.546**                        | 0.504                            | 0.503                        | 0.562                        | **0.609**                    |

</div>

CIs are 95% bootstrap over the 50 queries (B=1000); they're wide because each Nano dataset has only 50 queries, which is exactly why most single-dataset static-vs-BM25 gaps aren't significant. Bold marks the BM25 wins/losses that survive the paired Wilcoxon test (HotpotQA, Touche2020 for BM25; Quora for static). Note `bge-small` leads the mean (0.609) but at ~22ms/query vs static's ~0.13ms.

</details>

## The Defining Limitation: Word Order

Here's the mechanism behind the quality gap, and it's a clean one. A static sentence vector is `mean(lookup(t) for t in tokens)`. Mean-pooling is **permutation-invariant**: reorder the tokens and the average is byte-for-byte identical. The model literally cannot tell two reorderings of the same words apart.

Drag the slider — shuffle the word order and watch the sentence vector stay exactly put:

<StaticEmbeddingDemo />

I measured this directly. For pairs that are pure reorderings of the same tokens, the static cosine is **exactly 1.0000**; a transformer, via attention and position encoding, pulls them apart — though by less than you might expect:

<PlotlyChart
  data={[
    {
      name: 'static-retrieval-mrl',
      orientation: 'h',
      y: [
        '"tech giant acquired rival"<br>vs reversed',
        '"dog bites man"<br>vs "man bites dog"',
        '"cat chased mouse"<br>vs reversed',
        '"surged" vs "plunged"<br>(words differ)',
      ],
      x: [1.0, 1.0, 1.0, 0.8],
      type: 'bar',
      marker: { color: '#10b981' },
      text: ['1.000', '1.000', '1.000', '0.80'],
      textposition: 'outside',
    },
    {
      name: 'all-MiniLM (transformer)',
      orientation: 'h',
      y: [
        '"tech giant acquired rival"<br>vs reversed',
        '"dog bites man"<br>vs "man bites dog"',
        '"cat chased mouse"<br>vs reversed',
        '"surged" vs "plunged"<br>(words differ)',
      ],
      x: [0.985, 0.907, 0.983, 0.727],
      type: 'bar',
      marker: { color: '#6366f1' },
      text: ['0.985', '0.907', '0.983', '0.727'],
      textposition: 'outside',
    },
  ]}
  layout={{
    title: { text: 'Cosine similarity: can the model tell these apart?', font: { size: 14 } },
    barmode: 'group',
    xaxis: { title: 'cosine (1.0 = identical, lower = distinguished)', range: [0, 1.15] },
    yaxis: { autorange: 'reversed' },
    height: 420,
    margin: { t: 48, r: 32, b: 56, l: 150 },
    legend: { orientation: 'h', y: -0.18 },
  }}
/>

Two things worth noting. First, the transformer is only _approximately_ order-sensitive on short sentences — "dog bites man" vs "man bites dog" still scores 0.91, not far from the static 1.000. Mean-pooling over contextualized tokens washes out a lot of word order even in a transformer. Second, and more useful: the bottom row ("surged" vs "plunged") swaps a _word_, not the order — and there the static model (0.80) does about as well as the transformer (0.73).

So the cost of going static is specifically **word order and composition**, not vocabulary. If your relevance signal lives in which words appear — which is most of retrieval — static holds up. If it lives in how they're arranged ("flight NY→London" vs "London→NY", negation, role reversal), static is blind by construction.

## Matryoshka: Quality vs Dimension

`static-retrieval-mrl-en-v1` is trained with [Matryoshka representation learning](https://huggingface.co/blog/matryoshka), so you can truncate the 1024-d vector to fewer dimensions and just re-normalize — no re-encoding. Since static models have no depth (no layers), this dimension axis is the closest thing they have to a quality knob. Here's how NDCG@10 degrades as you shrink it:

<PlotlyChart
  data={[
    {
      x: [1024, 512, 256, 128, 64, 32],
      y: [0.5935, 0.5759, 0.5644, 0.535, 0.4587, 0.3093],
      mode: 'lines+markers+text',
      type: 'scatter',
      text: ['0.594', '0.576', '0.564', '0.535', '0.459', '0.309'],
      textposition: 'top center',
      marker: { size: 9, color: '#10b981' },
      line: { color: '#10b981', width: 2 },
    },
  ]}
  layout={{
    title: { text: 'static-retrieval-mrl: NDCG@10 vs Matryoshka dimension', font: { size: 14 } },
    xaxis: {
      title: 'embedding dimension',
      type: 'log',
      tickmode: 'array',
      tickvals: [32, 64, 128, 256, 512, 1024],
      ticktext: ['32', '64', '128', '256', '512', '1024'],
    },
    yaxis: { title: 'NDCG@10', range: [0.25, 0.65] },
    height: 360,
    margin: { t: 48, r: 24, b: 56, l: 64 },
    showlegend: false,
  }}
/>

Truncating to **256-d keeps 95% of full quality at a quarter of the storage** (0.564 vs 0.594). The curve stays gentle down to 128-d, then falls off a cliff: 64-d drops to 77% of full, and 32-d to just 52%. The sweet spot for this model is 256–512 dimensions — small enough to be cheap, large enough to dodge the cliff.

## Can Fuzzy Keyword Matching Beat Plain Fusion? (No)

Mean-pooling throws away term identity. A tempting fix sits _between_ sparse retrieval and [ColBERT](https://arxiv.org/abs/2004.12832): keep each token as a "keyword," but match keywords by **vector similarity** instead of exact string match, then score BM25-style. That's roughly [COIL](https://arxiv.org/abs/2104.07186) / late-interaction — and the static table hands you the token vectors for free, no transformer. I built the transformer-free versions and benchmarked them on all 13 NanoBEIR datasets against the one baseline everyone should try first: **trivial score fusion of BM25 + static-dense.**

<PlotlyChart
  data={[
    {
      x: [
        'BM25 + static-dense<br>(fuse)',
        'BM25',
        'query expansion',
        'static MaxSim',
        'doc-side SPLADE',
        'static-dense',
      ],
      y: [0.564, 0.546, 0.531, 0.507, 0.503, 0.504],
      type: 'bar',
      marker: { color: ['#10b981', '#f59e0b', '#94a3b8', '#94a3b8', '#94a3b8', '#6366f1'] },
      text: ['0.564', '0.546', '0.531', '0.507', '0.503', '0.504'],
      textposition: 'outside',
    },
  ]}
  layout={{
    title: {
      text: 'Mean NDCG@10 over 13 NanoBEIR (all transformer-free, CPU)',
      font: { size: 14 },
    },
    xaxis: { title: '' },
    yaxis: { title: 'NDCG@10', range: [0.45, 0.59] },
    height: 380,
    margin: { t: 48, r: 24, b: 64, l: 56 },
    showlegend: false,
  }}
/>

The result is unambiguous, and it's a negative one for the clever methods:

- **Trivial fusion wins.** Min-max fusing BM25 with static-dense cosine reaches **0.564** vs BM25's 0.546 (+0.018, p=0.009, stable across fusion weights α∈[0.35,0.8] on 13 datasets) — and beats _both_ its parents (static-dense alone is 0.504). BM25 and dense fail on anti-correlated tasks (lexical vs semantic), so fusing them is close to ideal.
- **Query-side expansion is a wash** (0.531, _below_ BM25). Not because expansion does nothing — because it does opposite things on different tasks:

<PlotlyChart
  data={[
    {
      orientation: 'h',
      y: [
        'FEVER',
        'Touche2020',
        'HotpotQA',
        'MSMARCO',
        'DBPedia',
        'ArguAna',
        'FiQA',
        'SCIDOCS',
        'ClimateFEVER',
        'NQ',
        'SciFact',
        'NFCorpus',
        'Quora',
      ],
      x: [
        -0.086, -0.069, -0.048, -0.029, -0.017, -0.014, -0.007, 0.011, 0.016, 0.02, 0.027, 0.041,
        0.067,
      ],
      type: 'bar',
      marker: {
        color: [
          '#ef4444',
          '#ef4444',
          '#ef4444',
          '#ef4444',
          '#ef4444',
          '#ef4444',
          '#ef4444',
          '#10b981',
          '#10b981',
          '#10b981',
          '#10b981',
          '#10b981',
          '#10b981',
        ],
      },
    },
  ]}
  layout={{
    title: { text: 'Query expansion lift over BM25, per dataset (NDCG@10)', font: { size: 14 } },
    xaxis: {
      title: '← hurts (lexical / multi-hop)   ·   helps (semantic / QA) →',
      zeroline: true,
      zerolinewidth: 2,
    },
    yaxis: { autorange: 'reversed' },
    height: 420,
    margin: { t: 48, r: 24, b: 56, l: 96 },
    showlegend: false,
  }}
/>

Expansion helps paraphrase/QA tasks (Quora +0.07, NFCorpus +0.04, SciFact +0.03) and hurts lexical/multi-hop ones (FEVER −0.09, Touche −0.07, HotpotQA −0.05) by roughly equal amounts. You can't know the task type a priori, so the average is nothing — and IDF-gating (don't expand rare identity terms) doesn't separate the two.

- **The heavier mechanisms are actively worse.** Doc-side expansion (the SPLADE direction) floods documents with neighbors and drops to 0.503; **static MaxSim** (ColBERT minus the learned encoder) collapses toward bag-of-tokens at 0.507 — both significantly below BM25 (p&lt;1e-5). Tellingly, both _beat_ BM25 on MS MARCO and Quora — the distribution `potion-retrieval` was trained on — but lose badly everywhere precision matters.

The lesson: a static table's **fixed neighbor geometry** is enough to _add_ to a real retriever (the fusion win) but not to _be_ one. What real SPLADE and ColBERT have that this lacks is **learned, contextual** expansion weights; selection, pooling, and gating over an off-the-shelf geometry can't recover it. (Whether _learned-but-static_ weights would close the gap is untested — but the systemic, un-gateable losses on lexical tasks suggest the geometry itself, not just the weighting, is the limit.) If you want cheap gains over BM25 from a static model: **fuse, don't fuzz.**

## Where Static Embeddings Fit

Static embeddings are to dense retrieval what BM25 is to sparse: the **no-model baseline**. The whole spectrum, from the SPLADE post and this one:

- **Sparse:** BM25 (no model) → inference-free SPLADE (model at index only) → full SPLADE (model both sides)
- **Dense:** static embeddings (tiny lookup, no transformer) → … → full transformer dense

Reach for static when: latency or CPU/GPU budget is the binding constraint, the corpus is large and re-encoding cost matters, you need an embedding you can run anywhere (edge, mobile, a Lambda with no GPU), **or your task has low lexical overlap** (paraphrase, argument, question→passage) where it beats BM25 outright. A 1,500 docs/sec CPU encoder that needs no accelerator is a genuinely different operational story from a transformer.

Skip static when: relevance depends on word order or composition, the corpus is lexically dense enough that BM25 already wins (and BM25 is free), or you have the GPU budget and the task needs a real transformer to clear BM25 at all (fiqa was this case — MiniLM 0.36 vs static 0.20 vs BM25 0.23).

## Limitations

**Small datasets, weak statistics.** The detailed scifact run is one dataset (300 queries); the 4-dataset comparison is hand-picked; the NanoBEIR sweep is 13 datasets at 50 queries each. With 50 queries, only the largest gaps are significant, and the lexical-overlap correlation (ρ=−0.43, n=13) is suggestive, not conclusive. A full-size BEIR run (thousands of queries) would tighten the CIs and could move the correlation either way.

**Use the retrieval-tuned model.** `potion-base-32M` is distilled on general C4 text and trails its retrieval fine-tune `potion-retrieval-32M` by ~13% NDCG. The base model is the wrong thing to benchmark for search — a mistake easy to make since it's the "default" potion.

**Order blindness is structural, not tunable.** No amount of dimension or training fixes permutation invariance — it's baked into mean-pooling a lookup table. Models that need word order can't be static.

**Vocabulary is frozen at training time.** A static table can't adapt to truly novel terms at query time; out-of-vocabulary tokens fall back to subword averages. potion uses its own vocabulary (512-d) while static-mrl reuses BERT's (1024-d) — compare them by quality and latency, not by dimension.

## Key Takeaways

- **Static embeddings drop the transformer entirely** — tokenize, look up one vector per token, average. The retrieval-tuned `potion-retrieval-32M` hits **97.9% of all-MiniLM's NDCG@10 on scifact (and beats its Recall@10) at ~90× lower query latency and ~80× faster indexing**, all on CPU.
- **Static is roughly BM25-competitive across tasks, with a mild lean toward semantic ones.** Over 13 NanoBEIR datasets it averages ≈92% of BM25's NDCG@10 and wins on 6/13; the "wins when lexical overlap is low" trend is real but weak (ρ=−0.43, p=0.14) — Quora (high overlap, static still wins) shows task _type_ matters more than a raw overlap score.
- **Use the retrieval-tuned variant.** `potion-retrieval-32M` beats general-purpose `potion-base-32M` by ~13% NDCG — same table, fine-tuned on MS MARCO/QA data. Benchmarking the base model understates static retrieval badly.
- **Static embeddings are exactly word-order blind.** Any reordering of the same tokens yields a byte-identical vector (cosine 1.000). A transformer is only _approximately_ order-sensitive. The cost is composition, not vocabulary — where words _differ_, static ≈ transformer.
- **Matryoshka truncation to 256-d keeps 95% of quality** at a quarter of the storage; below 128-d it falls off a cliff (64-d → 77%, 32-d → 52%).
