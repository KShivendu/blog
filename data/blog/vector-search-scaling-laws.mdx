---
title: 'Scaling laws for vector search'
date: '2025-11-29'
lastmod: '2025-11-29'
tags: ['search', 'internals', 'infra']
draft: true
summary: 'Measuring how brute-force vector search latency scales with dataset size, and what binary quantization actually buys you versus the napkin math.'
images: ['/static/images/blogs/exa-clean.jpg']
authors: ['default']
---

![Exa Napkin Math Blog Cover Image](/static/images/blogs/exa-clean.jpg)

Latency vs Recall

We'll study:

- Effect of increasing dataset size: `range(0, 10M, 100M, 1B)`
- Effect of increasing dims

We'll do it with the following:

- Effect of binary quantization

And we'll do this with two different approaches:

- Brute force vector comparisons (with SIMD optimizations)
- HNSW

## Results: brute-force scaling

First data points, measured with [`bfb`](https://github.com/qdrant/bfb) against an **isolated** Qdrant node (16-core / 64 GB, ARM; vectors in RAM, 8 segments), with HNSW disabled (`indexing_threshold = 0`) so every query is a full scan. Random 256-dim vectors, server-side p50, 8 concurrent queries (`-p 8`), 2000 queries per point (500 for the ≥5M cells). This is a single run — treat the numbers as preliminary.

<PlotlyChart
  data={[
    {
      name: 'fp32 full scan',
      x: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      y: [33.2, 76.1, 185.5, 365.3, 438.0, 1849.3, 1197.4],
      mode: 'lines+markers',
      type: 'scatter',
      marker: { size: 9, color: '#d6336c' },
      line: { width: 2, color: '#d6336c' },
    },
    {
      name: 'binary quant',
      x: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      y: [1.6, 28.7, 85.5, 147.1, 192.5, 281.5, 466.5],
      mode: 'lines+markers',
      type: 'scatter',
      marker: { size: 9, color: '#1c7ed6', symbol: 'square' },
      line: { width: 2, color: '#1c7ed6' },
    },
  ]}
  layout={{
    title: { text: 'Brute-force search latency vs dataset size (256-dim)', font: { size: 15 } },
    xaxis: {
      title: { text: 'dataset size (vectors)', standoff: 12 },
      type: 'log',
      tickmode: 'array',
      tickvals: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      ticktext: ['100k', '200k', '500k', '1M', '2M', '5M', '10M'],
    },
    yaxis: { title: 'server p50 latency (ms)', type: 'log' },
    height: 440,
    showlegend: true,
    legend: { orientation: 'h', y: -0.2 },
    margin: { t: 56, r: 32, b: 80, l: 64 },
  }}
/>

<PlotlyChart
  data={[
    {
      name: 'fp32 full scan',
      x: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      y: [184.6, 102.2, 42.8, 21.8, 19.0, 4.3, 6.5],
      mode: 'lines+markers',
      type: 'scatter',
      marker: { size: 9, color: '#d6336c' },
      line: { width: 2, color: '#d6336c' },
    },
    {
      name: 'binary quant',
      x: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      y: [413.9, 217.1, 86.9, 63.9, 45.6, 31.3, 14.4],
      mode: 'lines+markers',
      type: 'scatter',
      marker: { size: 9, color: '#1c7ed6', symbol: 'square' },
      line: { width: 2, color: '#1c7ed6' },
    },
  ]}
  layout={{
    title: { text: 'Throughput vs dataset size', font: { size: 15 } },
    xaxis: {
      title: { text: 'dataset size (vectors)', standoff: 12 },
      type: 'log',
      tickmode: 'array',
      tickvals: [100000, 200000, 500000, 1000000, 2000000, 5000000, 10000000],
      ticktext: ['100k', '200k', '500k', '1M', '2M', '5M', '10M'],
    },
    yaxis: { title: 'throughput (req/s)', type: 'log' },
    height: 440,
    showlegend: true,
    legend: { orientation: 'h', y: -0.2 },
    margin: { t: 56, r: 32, b: 80, l: 64 },
  }}
/>

| Vectors | fp32 p50 | Binary quant p50 | fp32 QPS | BQ QPS |
| ------- | -------- | ---------------- | -------- | ------ |
| 100k    | 33 ms    | 1.6 ms           | 185      | 414    |
| 200k    | 76 ms    | 29 ms            | 102      | 217    |
| 500k    | 186 ms   | 86 ms            | 43       | 87     |
| 1M      | 365 ms   | 147 ms           | 22       | 64     |
| 2M      | 438 ms   | 193 ms           | 19       | 46     |
| 5M      | 1849 ms  | 282 ms           | 4.3      | 31     |
| 10M     | 1197 ms  | 467 ms           | 6.5      | 14     |

I'm holding off on conclusions, because the large-N fp32 numbers don't behave cleanly yet:

- The fp32 curve is **not monotonic** — 5M (1849 ms) comes out *slower* than 10M (1197 ms), and this reproduces across runs even though the node, RAM residency, and segment count (8) are identical. The tails are heavy too (fp32 p99 is 4–5× p50).
- This is **not** noisy neighbours — the node is dedicated and nothing else is querying it. The likely cause is the measurement setup: 8 concurrent queries (`-p 8`), each fanning across 8 segments on 16 cores, is heavily oversubscribed, so per-query latency gets sensitive to scheduling. Binary quant is far steadier here since each scan touches ~32× less data.

So before reading any scaling "law" off this, the next step is to control for that — repeat each point, sweep concurrency, and likely pin the scan to a fixed parallelism so latency reflects data size rather than the scheduler. Numbers above will likely move.

### Time complexity analysis of HNSW vector indexing:

Each HNSW insertion needs a search, and assuming it takes K ops to add the found K links. This means insertion time is:

(O(log1) + k) + (O(log2) + k) + .... (O(logN) + k)

Sigma(log(I), i*from=1, i_to=N) + kN
= log(1 * 2 \_ 3 \* .... N) + kN
= log(N!) + kN

We have Stirling's approximation for large N:

log(N!)≈NlogN−N

log(N!)+kN≈NlogN−N+kN=NlogN+(k−1)N

Since K is constant, time complexity is O(NLogN) which is very bad

Why am I doing this? How it aligns with my life philosophy?

- I think napkin maths is a meta technique. It's very generalizable and very powerful. I want to see how well I can predict the system and its behaviour with the numbers I'll get.
- Although, this is good for
