// The header name's hover animation. Both variants live in ./matrix-name and
// are rendered side by side at /matrix-lab; this file picks the one that ships.
export { default } from './matrix-name/TerminalDecrypt'
